@model IEnumerable<IAS_Handyman.Models.Technician>

<head>
    <style>
        td:last-child {
            width: 100%;
        }
    </style>
</head>

<div class="modal-header">
    <h5 class="modal-title" style="float:left;">Seleccione un técnico</h5>
    <button id="btn_hide_client_modal" type="button" data-dismiss="modal" class="close" style="color:red; margin-right:5px">
        X
    </button>
</div>

@if (Model != null && Model.Count() > 0)
{
    <table class="table table-striped table-sm" style="display:block; overflow:auto">
        <thead style="background-color:lightgray;">
            <tr>
                <th></th>
                <th style="white-space:nowrap">
                    @Html.DisplayNameFor(model => model.Identification)
                </th>
                <th style="white-space:nowrap">
                    @Html.DisplayNameFor(model => model.FullName)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="white-space:nowrap">
                        <button class="btn btn-primary" onclick="selectTechnician(@item.Id)">Seleccionar</button>
                    </td>
                    <td style="white-space:nowrap">
                        @Html.DisplayFor(modelItem => item.Identification)
                    </td>
                    <td style="white-space:nowrap">
                        @Html.DisplayFor(modelItem => item.FullName)
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    // No items
    <div><label>No hay técnicos registrados</label></div>
}

